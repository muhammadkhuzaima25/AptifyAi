{
  "buildCommand": "cd client && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm --prefix client install && npm --prefix server install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
